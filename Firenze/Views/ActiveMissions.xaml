<UserControl x:Class="Firenze.Views.ActiveMissions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
            <ItemsControl ItemsSource="{Binding ActiveMissions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="10" CornerRadius="5" BorderBrush="Black" BorderThickness="2">
                            <StackPanel>
                                <TextBlock>Mission</TextBlock>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
    </StackPanel>
</UserControl>
